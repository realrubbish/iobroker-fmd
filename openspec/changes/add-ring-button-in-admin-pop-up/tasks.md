# Tasks: add-ring-button-in-admin-pop-up

## 1. Replace the `testConnection` schema item with `ringNow`

- [ ] 1.1 In `src-admin/schema.json5`, replace the `testConnection: { type: "sendTo", label: "Test Connection", command: "testConnection" }` form item in the Status panel with `ringNow: { type: "sendTo", label: "Ring Device", command: "ring" }`. The `command` MUST be `"ring"` (the string the adapter's `onMessage.ring` handler in `src/main.ts:505-555` accepts).
- [ ] 1.2 In `src-admin/schema.json5`, remove the `lastTestResult: { type: "staticText", label: "Last Test Result", text: "(click Test Connection to run)" }` form item. The Status panel now contains only `connectionState`, `ringNow`, and `lastError`.
- [ ] 1.3 In `src-admin/schema.json5`, update the header comment block (the `// `testResult` is a `staticText` line whose value is pushed in from `App.tsx`'s `testResult` state...` paragraph) to describe the new `ringNow` item, the payload contract, and the `window.alert` reply shape. Drop the references to `testResult` state and the inline-timestamp formatting (those are SPA-path only now).

## 2. Carry the `display` fix forward and update the App.tsx guard

- [ ] 2.1 In `src-admin/App.tsx`, verify the local-working-tree `display` fix from the previous change is in place: in the `setData((prev) => ({ ...prev, connectionState: ... }))` call inside the 5 s poll loop, the `connectionState` object MUST be `{ val: conn ? conn.val === true : false }` only — no `display` key. The `@iobroker/json-config` meta-schema rejects the `display` property on `staticText` items and the admin SPA skips the whole Status panel if any item fails validation. If the `display` line is missing in the working tree, restore it (it is the same edit that was applied in the local working tree during the previous change's 2026-06-11 E2E session).
- [ ] 2.2 In `src-admin/App.tsx`, rename the `hasSchemaTestConnection: boolean` module-level constant (computed via IIFE) to `hasSchemaRingNow: boolean`. The probe walks `jsonConfigSchema.items.status.items.ringNow.type === "sendTo"`. Add a `console.log` for the probe result so the next E2E session can confirm the schema shape.
- [ ] 2.3 In `src-admin/App.tsx`, rename the custom `<button>` label from `Test Connection` to `Ring Device`. Update the click handler: instead of `socket.sendTo(adapterName.instance, "testConnection", {})` it MUST be `socket.sendTo(adapterName.instance, "ring", { deviceId: <configured ringDeviceId> })`. The configured `ringDeviceId` is read from the same `data` prop that the `JsonConfig` already populates from `system.adapter.iobroker-fmd.0.native` (via `getAdapterConfig`). If `ringDeviceId` is empty, the handler SHOULD show a visible "Default Ring Device is empty" hint (e.g. via `setTestResult` or a transient toast — simplest: pass an empty deviceId and let the adapter-runtime reply with the error; the `window.alert` surfaces it).
- [ ] 2.4 In `src-admin/App.tsx`, gate the custom `<button>` block on `!hasSchemaRingNow` (renamed from `!hasSchemaTestConnection`). Update the inline JSX comment header to record the new fallback contract: "The custom button is a fallback for future admin versions that take the iframe path AND that have been forked to remove the `type: \"sendTo\"` schema item. The main path is the schema item, which is rendered in both the native form (admin 7.7.22) and the iframe path."

## 3. Build the admin UI

- [ ] 3.1 Run `npm run build:admin` to regenerate `admin/index.html`, `admin/index_m.html`, `admin/assets/`, and `admin/jsonConfig.json5` from the updated `src-admin/`. Confirm the build script copies the new `src-admin/schema.json5` (with the `ringNow` item and without `testConnection` / `lastTestResult`) to `admin/jsonConfig.json5` and the regenerated `index.html` still references the Vite entry script correctly.
- [ ] 3.2 Open `admin/jsonConfig.json5` after the build and verify it contains the new `ringNow: { type: "sendTo", label: "Ring Device", command: "ring" }` item and no `testConnection` / `lastTestResult` items. The file is the source the admin SPA reads at runtime, so this verification is the one that proves the build worked.

## 4. Update project docs

- [ ] 4.1 In `docs/admin-ui.md`, find the "Known limitation: admin 7.22 SPA renders native form" section. Update the "Workarounds for testing the SPA features manually while the iframe path is broken" list: remove the `Test Connection` entry entirely (it has been replaced by the `Ring Device` button, which is reachable in the native form). Replace it with: "Ring Device is reachable in the native form via the `type: \"sendTo\"` schema item. The reply is shown via `window.alert` (the ioBroker admin default). The button sends `ring` to the adapter with `payload: { deviceId: config.ringDeviceId }`; the adapter's `onMessage.ring` handler dispatches the ring. The inline `Last Ring Result: <msg> at HH:MM:SS` formatting remains gated behind the iframe path." Update the "Until a follow-up investigation ..." paragraph to drop the Test Connection mention.
- [ ] 4.2 In `docs/docker-development.md`, find the verify step ("Verify it Works"). Replace the existing "Click the `Test Connection` button" sentence with: "Click the `Ring Device` button in the wrench pop-up. The phone rings within ~2 s. The reply is shown via `window.alert` on admin 7.7.22 (native form) and inline on admin versions that take the iframe path. The adapter-side `onMessage.ring` handler in `src/main.ts` is the same in both cases."
- [ ] 4.3 In `docs/admin-ui-investigation-2026-06-08.md`, find the "Status update (2026-06-11)" section. Replace the "Test Connection button was missing from the pop-up" sentence with: "The Test Connection button was the wrong action for a wrench pop-up that already exposes the adapter's auth state and the `npm run auth:smoke` script for credential verification. The follow-up change `add-ring-button-in-admin-pop-up` re-purposes the Status-panel button slot to a `Ring Device` sendTo item (command: `ring`, payload: `{ deviceId: config.ringDeviceId }`). The live `0_userdata.0.FindMyDevice` device panel and the `App.tsx`-managed layout remain gated behind the iframe path and are tracked as a separate follow-up."

## 5. Deploy and verify in the dev container

> Per `CLAUDE.md` ("NEVER auto-commit or auto-push") and the OpenSpec
> rule that agents only implement features from an approved change, the
> git operations and the Docker dev cycle below are user-owned. The
> agent must stage, commit, and push the source + artefacts, then
> hand off; the user runs the install / upload / touch / verify steps.

- [ ] 5.1 *(user-owned)* Stage `src-admin/schema.json5`, `src-admin/App.tsx`, the regenerated `admin/` artefacts from task 3.1, the new openspec change artefacts (`openspec/changes/add-ring-button-in-admin-pop-up/{proposal,design,tasks}.md` and `specs/admin-ui/spec.md`), and the doc updates from task 4.
- [ ] 5.2 *(user-owned)* Commit with a Conventional Commits message of the form `fix(admin-ui): replace Test Connection with Ring Device button + apply display fix`. Co-author line per project convention.
- [ ] 5.3 *(user-owned)* Push the branch.
- [ ] 5.4 *(user-owned)* Run the Docker dev cycle from `CLAUDE.md` § "Deployment & Testing Workflow": bring the container up, install the adapter from GitHub, apply the directory workaround (re-apply if a `maintenance upgrade` wiped it — see `docs/docker-development.md`), upload, refresh the `io-package.json` cache via `touch`, and add or restart the instance.
- [ ] 5.5 *(user-owned)* Set `Default Ring Device` to the FMD deviceId (e.g. `eLZo3` for the user's single-device account) in the wrench pop-up, save, then click the new `Ring Device` button. Verify the phone rings within ~2 s and the reply is shown via `window.alert`. Confirm the adapter logs show `Ring state triggered for device: eLZo3` followed by `Ring command sent to device: eLZo3`.
- [ ] 5.6 *(user-owned)* Run `npm run auth:smoke` against the dev FMD server to confirm the underlying auth path is still healthy after the schema change. (The smoke test does not exercise the ring button; it is a belt-and-braces check that nothing in the jsonConfig update broke the build-time imports.)

## 6. Archive the change

- [ ] 6.1 *(user-owned)* After the verify step (5.5) confirms the ring button rings the phone, run `/opsx:archive` to archive the change. The archive process syncs the `admin-ui` capability delta spec to `openspec/specs/admin-ui/spec.md` and moves the change folder under `openspec/changes/archive/`. The 2026-06-11 "Status update" block in `docs/admin-ui-investigation-2026-06-08.md` (edited in task 4.3) will become the "post-archive" entry point for future readers. The `add-or-fix-test-button-in-admin-pop-up` change folder (historical, with its test-connection schema item and tasks.md) can be deleted at this point — it has been fully superseded by this change. The previous commit `72dc1bf` (the pushed test-connection change) should be reverted via a follow-up `revert:` commit (or kept on `main` as a known-bad commit if the user prefers — the working tree is now correct).
